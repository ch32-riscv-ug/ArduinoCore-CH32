name=CH32
version=1.0.0
author=ch32-riscv-ug
maintainer=ch32-riscv-ug
sentence=Register-level access to the CH32, and the examples for the built-in APIs.
paragraph=#include <CH32.h> brings in the register map, the GPIO helpers and the route tables for when the Arduino API does not reach far enough. That layer is deliberately not stable. The library also carries the examples for the built-in APIs, because a platform can only ship examples through a library.
category=Other
includes=CH32.h
url=https://github.com/ch32-riscv-ug/ArduinoCore-CH32
architectures=ch32v
